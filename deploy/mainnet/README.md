# Mainnet deployment (Gate M5)

Use the full runbook:

- `docs/MAINNET_GATE_M5.md`

This folder contains:

- `env.example` with all tunables
- hardened Docker Compose templates for seed and joiner
- optional systemd unit templates
- bootstrap + verification + proof collection scripts
