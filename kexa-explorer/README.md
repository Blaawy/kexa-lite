# kexa-explorer

Next.js 14 + TypeScript + Tailwind frontend for KEXA mainnet explorer.

## Setup

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Set `KEXA_RPC_URL` to your node HTTP endpoint.
